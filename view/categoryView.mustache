<section class="d-flex flex-column justify-content-center align-items-center text-center min-vh-100 p-3" style="background-color: {{categoria.color}};">

    <div class="position-absolute top-0 start-0 w-100 h-100"
         style="background-image: url('../../QuizGame/public/img/question-background.png'); background-size: cover; background-position: center; opacity: 0.7;">
    </div>

    <h2 class="mb-4 text-white z-1 fs-3 fs-md-2">¡Categoría seleccionada!</h2>

    <div class="row justify-content-center w-100 z-1">
        <div class="col-11 col-sm-8 col-md-6 col-lg-4">
            <div class="card shadow">
                <div class="card-body p-4">
                    <h3 class="fs-4">{{categoria.nombre}}</h3>
                    <p>Prepárate para responder preguntas de esta categoría.</p>
                    <form method="POST" action="/QuizGame/game/mostrarPregunta" class="d-grid">
                        <input type="hidden" name="categoria_id" value="{{categoria.id}}">
                        <button type="submit" class="btn btn-primary mt-3">Comenzar</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>