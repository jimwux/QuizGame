<section class="min-vh-100 d-flex flex-column bg-light py-5">
  <div class="container">
    <h1 class="mb-4 fw-bold text-center text-primary">🏆 Ranking de Usuarios</h1>

    <div class="table-responsive shadow rounded-4 bg-white">
      <table class="table table-hover align-middle mb-0">
        <thead class="table-primary text-center">
          <tr>
            <th scope="col" style="width: 10%;">Posición</th>
            <th scope="col" style="width: 55%;">Usuario</th>
            <th scope="col" style="width: 35%;">Puntaje Total</th>
          </tr>
        </thead>
        <tbody>
          {{#ranking}}
          <tr class="text-center">
            <td class="fw-bold fs-5">{{posicion}}</td>
            <td class="fw-semibold fs-5 text-success">
              <a href="/profile?username={{usuario}}" class="text-decoration-none text-primary fw-semibold">
                {{usuario}}
              </a>
            </td>
            <td class="fw-semibold fs-5 text-success">{{puntaje_total}}</td>
          </tr>
          {{/ranking}}
          {{^ranking}}
          <tr>
            <td colspan="3" class="text-center text-muted fst-italic py-4">
              No hay datos disponibles.
            </td>
          </tr>
          {{/ranking}}
        </tbody>
      </table>
    </div>
  </div>
</section>